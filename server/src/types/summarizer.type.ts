export type Chunk = {
  text: string;
  summary?: string;
  title?: string;
};

export type FilterSchemaType = {
    title: string;
    language: string;
    summaryType: string;
    pages: string;
}
